export type SiteConfig = typeof siteConfig;

export const siteConfig = {
	name: "PageBot",
	description: "GPT-4 Superpowers for your website in a single line of code",
	navItems: [

	],

	links: {

	},
};
